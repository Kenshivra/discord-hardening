[package]
name    = "age-verify-poc"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
blake3    = "1"
rand_core = { version = "0.6", features = ["getrandom"] }
thiserror = "1"
zeroize   = { version = "1", features = ["derive"] }

[profile.release]
opt-level = 3
strip     = true
